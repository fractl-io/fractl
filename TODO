* Find an alternative for `namespace`

* Integrate the store to the new runtime system (also see TODO in runtime/env.clj, L21 and test/compile.clj, L163)

* Integrate the new store and resolver protocols to the init process (exec.cljc).

* Add tests for various pattern use-cases, and get the default resolver working for them.

* Handle TODOs - parser/L12,26

* Cleanup code - remove old evaluator, all unused functions in all ns.